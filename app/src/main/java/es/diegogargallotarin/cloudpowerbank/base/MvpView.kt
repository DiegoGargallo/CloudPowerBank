package es.diegogargallotarin.cloudpowerbank.base

/**
 * Base interface for classes that act as the View of MVP.
 * Extend this interface for that specific View,
 * and implement it in an Activity or a Fragment.
 */
interface MvpView
